# AndroidX 迁移与启用开关
android.useAndroidX=true
android.nonTransitiveRClass=true

# Gradle JVM 参数。关闭增量类分析缓存，规避当前 Windows/JDK 的 JVM 编译器崩溃。
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:CICompilerCount=2
org.gradle.caching=false
org.gradle.vfs.watch=false
